import ScrollPreventer from './scroll-preventer';

const instance = new ScrollPreventer();

export const PreventScrolling = instance.PreventScrolling;
export const ReEnableScrolling = instance.ReEnableScrolling;
